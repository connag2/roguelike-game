const fs = require('fs'); const path = require('path'); const components = []; function walk(dir) { const files = fs.readdirSync(dir); files.forEach(f = const full = path.join(dir, f); if (fs.statSync(full).isDirectory()) walk(full); else if (f.endsWith('.js') 
